{{- if .Values.memoriaAgent.hpa.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: memoria-agent
  labels:
    {{- include "ai-architect.labels" . | nindent 4 }}
    app.kubernetes.io/component: memoria-agent
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: memoria-agent
  minReplicas: {{ .Values.memoriaAgent.hpa.minReplicas }}
  maxReplicas: {{ .Values.memoriaAgent.hpa.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.memoriaAgent.hpa.targetCPU }}
{{- end }}
