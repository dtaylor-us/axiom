apiVersion: v1
kind: Service
metadata:
  name: specweaver-api
  namespace: {{ .Release.Namespace }}
  labels:
    app: specweaver-api
    chart: {{ .Chart.Name }}
    release: {{ .Release.Name }}
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: {{ .Chart.Name }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/component: specweaver-api
  ports:
    - name: http
      port: 8082
      targetPort: 8082
      protocol: TCP
