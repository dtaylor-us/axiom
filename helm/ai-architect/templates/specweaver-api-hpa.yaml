{{- if .Values.specweaverApi.hpa.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: specweaver-api
  labels:
    {{- include "ai-architect.labels" . | nindent 4 }}
    app.kubernetes.io/component: specweaver-api
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: specweaver-api
  minReplicas: {{ .Values.specweaverApi.hpa.minReplicas }}
  maxReplicas: {{ .Values.specweaverApi.hpa.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.specweaverApi.hpa.targetCPU }}
{{- end }}
