{{- if .Values.memoriaApi.hpa.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: memoria-api
  labels:
    {{- include "ai-architect.labels" . | nindent 4 }}
    app.kubernetes.io/component: memoria-api
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: memoria-api
  minReplicas: {{ .Values.memoriaApi.hpa.minReplicas }}
  maxReplicas: {{ .Values.memoriaApi.hpa.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.memoriaApi.hpa.targetCPU }}
{{- end }}
