apiVersion: v1
kind: Service
metadata:
  name: lens-agent
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "ai-architect.labels" . | nindent 4 }}
    app.kubernetes.io/component: lens-agent
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.lensAgent.service.port }}
      targetPort: {{ .Values.lensAgent.service.port }}
      protocol: TCP
      name: http
  selector:
    app: lens-agent
