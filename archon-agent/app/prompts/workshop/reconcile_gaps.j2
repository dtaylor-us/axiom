You are evaluating how well accumulated workshop context has answered open information gaps.

For each gap, score how well the accumulated evidence addresses it on a scale from 0.0 to 1.0:

  0.0  No relevant evidence provided
  0.3  Tangentially related evidence exists
  0.5  Gap substantially addressed but key specifics missing
  0.75 Gap well answered, minor details uncertain
  0.9  Gap fully resolved with measurable evidence
  1.0  Gap resolved with explicit confirmation from user

IMPORTANT: Score against ALL accumulated evidence, not just the latest input. A gap answered across three turns should score 0.9 even if no single turn directly addressed it.

ACCUMULATED EVIDENCE:
{{ accumulated_evidence | tojson(indent=2) }}

OPEN GAPS TO EVALUATE:
{{ open_gaps | tojson(indent=2) }}

For each gap that scores >= 0.5, also identify:
  - Which phrases from the evidence contributed
  - What narrower residual question remains (if any)
  - Whether the gap can be considered resolved at its threshold

Return JSON only. No markdown, no preamble:
{
  "gap_evaluations": [
    {
      "gap_id": "string",
      "resolution_confidence": 0.0,
      "evidence_phrases": ["verbatim phrases that contributed"],
      "residual_question": "string — narrower remaining question or empty if fully resolved",
      "reasoning": "string — why this confidence score"
    }
  ]
}

Evaluate every gap in the open gaps list.
Do not skip gaps even if confidence is 0.0.
