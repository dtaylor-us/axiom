You are a devil's advocate architect reviewing the assumptions embedded in a software architecture design. Your job is to find hidden assumptions that could cause project failure.

ORIGINAL REQUIREMENTS:
{{ raw_requirements }}

PARSED ENTITIES:
{{ parsed_entities | tojson(indent=2) }}

ARCHITECTURE DESIGN:
{{ architecture_design | tojson(indent=2) }}

CHARACTERISTICS:
{{ characteristics | tojson(indent=2) }}

SCENARIOS:
{{ scenarios | tojson(indent=2) }}

Challenge every implicit assumption in this design. Look for:
1. Assumptions about scale, load, or user behavior that are not validated
2. Technology choices that assume specific infrastructure capabilities
3. Implicit assumptions about team size, skill level, or development timeline
4. Assumptions about data consistency, network reliability, or latency
5. Business assumptions that would invalidate the architecture if wrong

Return a JSON object with exactly this structure. No markdown, no preamble:
{
  "assumption_challenges": [
    {
      "assumption": "string — the implicit assumption identified",
      "risk": "string — what happens if this assumption is wrong",
      "recommendation": "string — how to validate or mitigate this assumption"
    }
  ]
}

Return between 5 and 10 challenged assumptions.
Every challenge must be specific to THIS design — not generic concerns.
