package com.specweaver.api.domain.model;

/**
 * Supported source document formats for requirements ingestion.
 */
public enum DocumentType {
    PLAIN_TEXT,
    MARKDOWN,
    PDF,
    DOCX,
    EMAIL
}
