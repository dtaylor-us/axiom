# Local development overrides
# axiom-api is under the 'platform' Compose profile
# and only starts when explicitly included.
# archon-api must be running before axiom-api starts.

axiom:
  pillars:
    archon:
      base-url: http://archon-api:8081
      enabled: true
